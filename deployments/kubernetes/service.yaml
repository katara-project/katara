---
apiVersion: v1
kind: Service
metadata:
  name: katara-core
  labels:
    app: katara-core
spec:
  type: ClusterIP
  selector:
    app: katara-core
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
