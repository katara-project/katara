{"id":"diff-01","intent":"review","raw":"diff --git a/src/auth/middleware.rs b/src/auth/middleware.rs\nindex 3a2f1b..8d9c42 100644\n--- a/src/auth/middleware.rs\n+++ b/src/auth/middleware.rs\n@@ -1,6 +1,7 @@\n use axum::{\n     extract::State,\n-    http::StatusCode,\n+    http::{Request, StatusCode},\n+    middleware::Next,\n     response::IntoResponse,\n };\n \n@@ -14,10 +15,22 @@ use crate::AppState;\n \n-pub async fn auth(req: Request, next: Next) -> impl IntoResponse {\n-    // TODO: implement auth\n-    next.run(req).await\n+pub async fn auth(\n+    State(state): State<Arc<AppState>>,\n+    req: Request<Body>,\n+    next: Next,\n+) -> Result<impl IntoResponse, StatusCode> {\n+    let token = req\n+        .headers()\n+        .get(AUTHORIZATION)\n+        .and_then(|v| v.to_str().ok())\n+        .and_then(|v| v.strip_prefix(\"Bearer \"))\n+        .unwrap_or(\"\");\n+\n+    if state.api_keys.contains(token) {\n+        Ok(next.run(req).await)\n+    } else {\n+        Err(StatusCode::UNAUTHORIZED)\n+    }\n }\ndiff --git a/src/auth/middleware.rs b/src/auth/middleware.rs\n--- a/tests/auth_test.rs\n+++ b/tests/auth_test.rs\n@@ -0,0 +1,28 @@\n+#[tokio::test]\n+async fn valid_token_passes() {\n+    let app = build_test_app().await;\n+    let res = app\n+        .oneshot(Request::builder()\n+            .uri(\"/v1/compile\")\n+            .header(\"Authorization\", \"Bearer test-key-123\")\n+            .body(Body::empty()).unwrap())\n+        .await.unwrap();\n+    assert_eq!(res.status(), StatusCode::OK);\n+}\n+\n+#[tokio::test]\n+async fn missing_token_is_401() {\n+    let app = build_test_app().await;\n+    let res = app\n+        .oneshot(Request::builder()\n+            .uri(\"/v1/compile\")\n+            .body(Body::empty()).unwrap())\n+        .await.unwrap();\n+    assert_eq!(res.status(), StatusCode::UNAUTHORIZED);\n+}","compiled":"[k:review]auth/middleware.rs: replace TODO stub with Bearer token check using state.api_keys.contains(). Added Request<Body>+Next params, returns 401 on miss. tests/auth_test.rs: 2 new tests (valid_token_passes, missing_token_is_401).","raw_tokens_estimate":294,"compiled_tokens_estimate":46,"token_reduction_pct":84.4}
{"id":"diff-02","intent":"review","raw":"diff --git a/Cargo.toml b/Cargo.toml\nindex aa1234..bb5678 100644\n--- a/Cargo.toml\n+++ b/Cargo.toml\n@@ -10,6 +10,7 @@ serde = { version = \"1\", features = [\"derive\"] }\n serde_json = \"1\"\n tokio = { version = \"1\", features = [\"full\"] }\n reqwest = { version = \"0.12\", features = [\"json\"] }\n+fnv = \"1\"\n \ndiff --git a/fingerprint/src/lib.rs b/fingerprint/src/lib.rs\nindex cc1111..dd2222 100644\n--- a/fingerprint/src/lib.rs\n+++ b/fingerprint/src/lib.rs\n@@ -1,8 +1,10 @@\n-use std::collections::hash_map::DefaultHasher;\n-use std::hash::{Hash, Hasher};\n+use fnv::FnvHasher;\n+use std::hash::{Hash, Hasher};\n \n pub fn fingerprint(input: &str) -> u64 {\n-    let mut hasher = DefaultHasher::new();\n+    let mut hasher = FnvHasher::default();\n     input.trim().to_lowercase().hash(&mut hasher);\n     hasher.finish()\n }","compiled":"[k:review]Cargo.toml: +fnv=\"1\". fingerprint/src/lib.rs: DefaultHasher → FnvHasher::default() for stable cross-version hashing. Behavior unchanged, hash values differ.","raw_tokens_estimate":137,"compiled_tokens_estimate":28,"token_reduction_pct":79.6}
