[package]
name = "fingerprint"
description = "Prompt fingerprint graph — stable hashing for deduplication"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
fnv = { workspace = true }
